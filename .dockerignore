# Git
.git
.gitignore

# Terraform (Keep these OUT of your image)
terraform/
*.tfstate
*.tfstate.backup
.terraform/

# Environment & Secrets
.env
__pycache__/
*.pyc

# Local dbt artifacts (Let the container build its own)
dbt/target/
dbt/dbt_packages/
dbt/logs/
